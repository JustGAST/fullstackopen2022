import theme from './theme.js';

const ui = {
  button: {
    padding: 15,
    backgroundColor: theme.colors.primary,
    borderRadius: 5,
  },
  buttonText: {
    textAlign: "center"
  }
}

export default ui;